% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Main_TJ_to_factor_list.R
\name{Tj_factor_list}
\alias{Tj_factor_list}
\title{将因子信息数据框转换为因子水平与标签的列表}
\usage{
Tj_factor_list(factor_df, search_name = T)
}
\arguments{
\item{factor_df}{数据框（data.frame），必须包含三列：
\itemize{
\item "最终变量名"：字符串类型，存储变量的名称（如示例中的"diffweek"）
\item "因子水平"：存储因子的原始水平值（如示例中的0、1、2）
\item "因子标签"：字符串类型，存储对应因子水平的标签（如示例中的"第0周"、"第1周"、"第2周"）
}}
}
\value{
一个列表，列表的每个元素对应"最终变量名"中的一个唯一变量。每个元素本身也是一个列表，包含两个向量：
\itemize{
\item levels：对应变量的因子水平向量（取自"因子水平"列）
\item labels：对应变量的因子标签向量（取自"因子标签"列），与levels顺序一一对应
}
}
\description{
该函数用于处理包含变量因子信息的数据框，提取每个变量对应的因子水平及其标签，
并将结果组织为列表形式，方便后续将变量转换为带标签的因子时使用。
}
\examples{
# 创建示例数据框
factor_info <- data.frame(
  最终变量名 = c("diffweek", "diffweek", "diffweek"),
  因子水平 = c(0, 1, 2),
  因子标签 = c("第0周", "第1周", "第2周"),
  stringsAsFactors = FALSE
)

# 调用函数
factor_list <- Tj_factor_list(factor_info)

# 查看结果
print(factor_list)
# 输出：
# $diffweek
# $diffweek$levels
# [1] 0 1 2
#
# $diffweek$labels
# [1] "第0周" "第1周" "第2周"
}
