% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/A7.1_SuperWord02.R
\name{SuperWord02}
\alias{SuperWord02}
\title{SuperWord02}
\usage{
SuperWord02(
  model_list = NULL,
  data_list = NULL,
  output_path,
  table_comments = NULL,
  introduction = "",
  report_text = NULL,
  show_stat_table = TRUE,
  hline.bottom.table = 1.5,
  hline.top.table = 1.5,
  hline.middle.table = 0.75,
  text.title.size = 14,
  text.heading1.size = 13,
  text.heading2.size = 12,
  text.intro.size = 12,
  text.textcom.size = 12,
  text.table.caption.size = 12,
  text.table.content.size = 11,
  text.table.note.size = 10,
  line.title.spacing = 1,
  line.heading1.spacing = 1.2,
  line.heading2.spacing = 1.1,
  line.intro.spacing = 1.3,
  line.textcom.spacing = 1.3,
  line.caption.spacing = 1,
  line.table.note.spacing = 1.2,
  text.title.family = "Times New Roman",
  text.heading1.family = "Times New Roman",
  text.heading2.family = "Times New Roman",
  text.intro.family = "Times New Roman",
  text.textcom.family = "Times New Roman",
  text.table.caption.family = "Times New Roman",
  text.table.content.family = "Times New Roman",
  text.table.note.family = "Times New Roman",
  pad.table.cell = 1,
  page_orientation = c("portrait", "landscape")
)
}
\arguments{
\item{model_list}{列表，可选。
结果模型对象。当前版本要求其中包含 \code{Result} 元素，
即函数会使用 \code{model_list$Result} 作为最终表格数据源。}

\item{data_list}{列表，可选。
直接传入的结果表列表。当 \code{model_list} 不可用时使用。
通常每个元素应为一个结果对象，且至少包含 \code{cross_table}，
例如：
\code{list("Table A" = list(cross_table = df1), "Table B" = list(cross_table = df2))}。}

\item{output_path}{字符型，必填。
输出 Word 文件路径，建议以 \code{.docx} 结尾，
例如 \code{"D:/report/super_table_report.docx"}。}

\item{table_comments}{列表，可选。
预留参数。当前版本函数体中尚未实际使用该参数。}

\item{introduction}{字符型，默认 \code{""}。
报告开头的引言或背景描述文字。若为空则不写入引言部分。}

\item{report_text}{列表，可选。
自动文字报告对象。若包含 \code{Methods}、\code{Results}、\code{TableNotes}
三个命名元素，则启用自动报告模式。

推荐结构如下：
\describe{
\item{Methods}{统计方法部分。可包含：
\code{heading1}、\code{chinese}、\code{english}}
\item{TableMethod}{统计方法表列表。仅在 \code{show_stat_table = TRUE} 时使用；
每个元素应为 data.frame}
\item{Results}{各结果模块的说明文字列表。
每个表名下可包含 \code{chinese} 和 \code{english}}
\item{TableNotes}{各结果表的表注列表。
每个表名下可包含 \code{chinese} 和 \code{english}}
}}

\item{show_stat_table}{逻辑值，默认 \code{TRUE}。
是否在 Word 报告中插入“统计方法表”。
若为 \code{TRUE}，则要求 \code{report_text$TableMethod} 存在且非空。}

\item{hline.bottom.table}{数值，默认 \code{1.5}。
表格底部边框线宽。}

\item{hline.top.table}{数值，默认 \code{1.5}。
表格顶部边框线宽。}

\item{hline.middle.table}{数值，默认 \code{0.75}。
表头下方中间横线线宽。}

\item{text.title.size}{报告总标题字号，默认 \code{14}。}

\item{text.heading1.size}{一级标题字号，默认 \code{13}。}

\item{text.heading2.size}{二级标题字号，默认 \code{12}。}

\item{text.intro.size}{引言正文字号，默认 \code{12}。}

\item{text.textcom.size}{结果说明文字字号，默认 \code{12}。}

\item{text.table.caption.size}{表题字号，默认 \code{12}。}

\item{text.table.content.size}{表格内容字号，默认 \code{11}。}

\item{text.table.note.size}{表注字号，默认 \code{10}。}

\item{line.title.spacing}{报告总标题行距，默认 \code{1}。}

\item{line.heading1.spacing}{一级标题行距，默认 \code{1.2}。}

\item{line.heading2.spacing}{二级标题行距，默认 \code{1.1}。}

\item{line.intro.spacing}{引言正文行距，默认 \code{1.3}。}

\item{line.textcom.spacing}{结果说明文字行距，默认 \code{1.3}。}

\item{line.caption.spacing}{表题行距，默认 \code{1}。}

\item{line.table.note.spacing}{表注行距，默认 \code{1.2}。}

\item{text.title.family}{报告总标题字体，默认 \code{"Times New Roman"}。}

\item{text.heading1.family}{一级标题字体，默认 \code{"Times New Roman"}。}

\item{text.heading2.family}{二级标题字体，默认 \code{"Times New Roman"}。}

\item{text.intro.family}{引言字体，默认 \code{"Times New Roman"}。}

\item{text.textcom.family}{结果说明文字字体，默认 \code{"Times New Roman"}。}

\item{text.table.caption.family}{表题字体，默认 \code{"Times New Roman"}。}

\item{text.table.content.family}{表格内容字体，默认 \code{"Times New Roman"}。}

\item{text.table.note.family}{表注字体，默认 \code{"Times New Roman"}。}

\item{pad.table.cell}{数值，默认 \code{1}。
表格单元格内边距。}

\item{page_orientation}{页面方向，默认 \code{c("portrait", "landscape")}。
可选 \code{"portrait"} 或 \code{"landscape"}，内部通过 \code{match.arg()} 匹配。}
}
\value{
函数主要作用是将 Word 文档写入 \code{output_path}。
当前版本不显式返回分析结果对象，通常返回 \code{NULL}（不可见）。
}
\description{
生成结构化 Word 统计分析报告，支持自动插入方法描述、统计方法表、结果解读文本、
主结果表格以及表注内容，并可统一控制标题、正文、表格、表注的字体、字号、
行距、页方向和表格边框样式。
}
\details{
\code{SuperWord02()} 适用于已经完成统计分析、并已整理出标准结果对象的场景。
函数会将结果对象中的表格内容提取出来，按照“报告标题 → 引言 →
统计学方法 → 统计方法表 → 各结果模块说明 → 结果表格 → 表注”的顺序，
输出为 \code{.docx} Word 报告。

函数主要流程如下：
\enumerate{
\item 检查 \code{flextable}、\code{officer}、\code{dplyr} 是否已安装；
\item 创建 Word 文档对象；
\item 根据 \code{page_orientation} 设置纵向或横向页面；
\item 解析数据源：优先使用 \code{model_list$Result}，否则使用 \code{data_list}；
\item 写入总标题 \code{"Statistical Report / 统计分析报告"}；
\item 若 \code{introduction} 非空，则写入引言；
\item 若 \code{report_text} 结构完整，则写入统计方法部分；
\item 若 \code{show_stat_table = TRUE}，写入 \code{report_text$TableMethod} 中的统计方法表；
\item 遍历每个结果模块，依次写入二级标题、结果说明、结果表格及表注；
\item 保存 Word 文件到 \code{output_path}。
}

表格格式规则：
\itemize{
\item 默认移除所有边框后，添加顶部线、表头下横线、底部线；
\item 表头加粗并居中；
\item 若表中存在 \code{row_id} 列，则按 \code{row_id == 1} 识别主变量行；
\item 若存在 \code{Variable} 列，则优先将其作为左侧标签列；
否则若存在 \code{ROW} 列，则使用 \code{ROW}；
若都不存在，则使用第一列作为标签列；
\item 非主变量行的标签列会自动增加左缩进。
}
}
\section{重要说明}{

\itemize{
\item 当前版本优先从 \code{model_list$Result} 提取结果表；若未提供，则使用 \code{data_list}。
\item \code{data_list} 或 \code{model_list$Result} 中的每个元素应包含 \code{cross_table}，
函数会使用 \code{final_data_list[[nm]]$cross_table} 作为最终展示表格。
\item 当 \code{report_text} 同时包含 \code{Methods}、\code{Results}、\code{TableNotes} 时，
才会启用自动文字报告模式。
\item 当 \code{show_stat_table = TRUE} 时，\code{report_text$TableMethod} 不能为空，
否则函数会报错：\verb{table_method is empty.}。
\item 参数 \code{table_comments} 当前版本已保留在接口中，但函数体中尚未实际使用。
}
}

\examples{
\dontrun{
# 方式1：直接使用结果表列表
SuperWord02(
  data_list = Result1$result$table$model.res,
  output_path = "D:/superword02_report.docx",
  introduction = "This report summarizes the statistical analysis results."
)

# 方式2：提供自动文字报告
SuperWord02(
  data_list = Result1$result$table$model.res,
  output_path = "D:/superword02_auto_report.docx",
  introduction = "This report summarizes the statistical analysis results.",
  report_text = Result1$result$output.infor$word.text.res,
  show_stat_table = TRUE
)

# 方式3：使用 model_list$Result
SuperWord02(
  model_list = list(Result = Result1$result$table$model.res),
  output_path = "D:/superword02_model_report.docx",
  show_stat_table = FALSE,
  page_orientation = "landscape"
)
}

}
\seealso{
\code{\link[flextable]{flextable}},
\code{\link[officer]{read_docx}},
\code{\link[officer]{body_add_flextable}}
}
