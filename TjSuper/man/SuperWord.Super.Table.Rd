% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/A7.1_SuperWord02.R
\name{SuperWord.Super.Table}
\alias{SuperWord.Super.Table}
\title{生成中英文双语 Word 统计报告}
\usage{
SuperWord.Super.Table(
  model_list = NULL,
  data_list = NULL,
  output_path,
  table_comments = NULL,
  introduction = "",
  report_text = NULL,
  show_stat_table = TRUE,
  hline.bottom.table = 1.5,
  hline.top.table = 1.5,
  hline.middle.table = 0.75,
  text.title.size = 14,
  text.heading1.size = 13,
  text.heading2.size = 12,
  text.intro.size = 12,
  text.textcom.size = 12,
  text.table.caption.size = 12,
  text.table.content.size = 11,
  text.table.superscript.size = 14,
  text.table.note.size = 10,
  line.title.spacing = 1,
  line.heading1.spacing = 1.2,
  line.heading2.spacing = 1.1,
  line.intro.spacing = 1.3,
  line.textcom.spacing = 1.3,
  line.caption.spacing = 1,
  line.table.note.spacing = 1.2,
  text.title.family = "Times New Roman",
  text.heading1.family = "Times New Roman",
  text.heading2.family = "Times New Roman",
  text.intro.family = "Times New Roman",
  text.textcom.family = "Times New Roman",
  text.table.caption.family = "Times New Roman",
  text.table.content.family = "Times New Roman",
  text.table.note.family = "Times New Roman",
  pad.table.cell = 1,
  page_orientation = c("portrait", "landscape"),
  bilingual_layout = c("cn_then_en", "legacy"),
  repeat_tables_in_english = FALSE
)
}
\arguments{
\item{model_list}{可选列表。通常为模型或统计分析函数的输出对象。
如果该对象中包含 \code{Result} 元素，则函数会使用 \code{model_list$Result}
作为报告表格来源。}

\item{data_list}{可选列表。直接提供的统计结果列表。列表中的每个元素
应包含一个 \code{cross_table} 数据框，用于生成 Word 报告中的表格。
当未提供有效的 \code{model_list$Result} 时，函数会使用 \code{data_list}。}

\item{output_path}{字符串。生成的 Word 文件保存路径，通常以 \code{.docx}
结尾。}

\item{table_comments}{可选列表。每张表格下方额外添加的注释文字。
如果该列表有名称，名称应与 \code{data_list} 或 \code{model_list$Result}
中的表格名称一致。}

\item{introduction}{字符串。报告开头的引言文字。}

\item{report_text}{可选列表。自动生成的报告文字。若提供，通常应包含
\code{Methods}、\code{Results} 和 \code{TableNotes} 三个元素，也可以包含
\code{TableMethod} 元素。}

\item{show_stat_table}{逻辑值。当 \code{report_text$TableMethod} 存在时，
是否在报告中显示统计方法表。默认为 \code{TRUE}。}

\item{hline.bottom.table}{数值。表格底部横线的宽度。}

\item{hline.top.table}{数值。表格顶部横线的宽度。}

\item{hline.middle.table}{数值。表头下方横线的宽度。}

\item{text.title.size}{数值。报告标题的字号。}

\item{text.heading1.size}{数值。一级标题的字号。}

\item{text.heading2.size}{数值。二级标题的字号。}

\item{text.intro.size}{数值。引言和统计方法正文的字号。}

\item{text.textcom.size}{数值。结果概述正文的字号。}

\item{text.table.caption.size}{数值。表格标题的字号。}

\item{text.table.content.size}{数值。表格主体内容的字号。}

\item{text.table.superscript.size}{数值。表格中上标标记的字号，
例如两两比较标记中的字母或星号。}

\item{text.table.note.size}{数值。表格注释文字的字号。}

\item{line.title.spacing}{数值。报告标题的行距。}

\item{line.heading1.spacing}{数值。一级标题的行距。}

\item{line.heading2.spacing}{数值。二级标题的行距。}

\item{line.intro.spacing}{数值。引言和统计方法正文的行距。}

\item{line.textcom.spacing}{数值。结果概述正文的行距。}

\item{line.caption.spacing}{数值。表格标题的行距。}

\item{line.table.note.spacing}{数值。表格注释文字的行距。}

\item{text.title.family}{字符串。报告标题字体。默认为
\code{"Times New Roman"}。}

\item{text.heading1.family}{字符串。一级标题字体。}

\item{text.heading2.family}{字符串。二级标题字体。}

\item{text.intro.family}{字符串。引言和统计方法正文字体。}

\item{text.textcom.family}{字符串。结果概述正文字体。}

\item{text.table.caption.family}{字符串。表格标题字体。}

\item{text.table.content.family}{字符串。表格主体内容字体。}

\item{text.table.note.family}{字符串。表格注释字体。}

\item{pad.table.cell}{数值。Word 表格单元格内边距。}

\item{page_orientation}{字符串。页面方向，可选 \code{"portrait"} 或
\code{"landscape"}。\code{"portrait"} 表示纵向页面，\code{"landscape"} 表示横向页面。}

\item{bilingual_layout}{字符串。双语报告布局方式，可选
\code{"cn_then_en"} 或 \code{"legacy"}。\code{"cn_then_en"} 表示先生成中文部分，
如果存在英文内容，再生成英文部分；\code{"legacy"} 表示兼容旧版本的
紧凑布局。}

\item{repeat_tables_in_english}{逻辑值。是否在英文部分重复显示详细表格。
默认为 \code{FALSE}。当设为 \code{FALSE} 时，英文部分通常只显示文字说明，
并提示详细表格见中文部分。}
}
\value{
不可见地返回 \code{output_path}。函数会在指定路径生成一个
Word \code{.docx} 文件。
}
\description{
\code{SuperWord.Super.Table()} 用于根据统计分析结果表自动生成 Microsoft Word
报告。该函数支持中文和英文双语报告布局、自动统计方法文字、
结果概述、表格标题、表格注释、字体设置、字号设置、行距设置、
表格边框设置以及页面方向设置。
}
\details{
该函数主要用于将一个或多个交叉表结果整理为规范的 Word 报告。
输入结果可以来自 \code{model_list$Result}，也可以直接通过 \code{data_list}
提供。

\code{data_list} 或 \code{model_list$Result} 中的每个元素应至少包含一个
\code{cross_table} 数据框。例如：

\preformatted{
data_list <- list(
  Baseline = list(
    cross_table = data.frame(...)
  )
)
}

如果表格中存在 \code{row_id} 列，函数会根据 \code{row_id} 对表格行进行格式化。
一般情况下，\code{row_id == 1} 的行会被视为主变量行，\code{row_id != 1}
的行会被视为分类水平行，并在 Word 表格中进行缩进显示。

如果表格中存在 p 值列，例如 \code{pvalue}、\code{p_value}、\code{p.value}、\code{p}
或 \code{p值}，函数会将该列的表头统一显示为斜体 \code{P}。

对于表格单元格中以字母或星号结尾的统计值，函数会尝试将末尾的
标记设置为上标格式。这通常用于显示两两比较标记。

该函数依赖 \code{officer} 和 \code{flextable} 包生成 Word 文档和格式化表格。
}
\examples{
\dontrun{
example_table <- data.frame(
  Variable = c("Age", "Sex", "Male", "Female"),
  Group1 = c("30.2 (5.1)", "", "50 (50.0\%)", "50 (50.0\%)"),
  Group2 = c("31.4 (4.8)", "", "45 (45.0\%)", "55 (55.0\%)"),
  P = c("0.120", "", "0.480", ""),
  row_id = c(1, 1, 2, 2),
  stringsAsFactors = FALSE
)

data_list <- list(
  Baseline = list(cross_table = example_table)
)

output_file <- tempfile(fileext = ".docx")

SuperWord.Super.Table(
  data_list = data_list,
  output_path = output_file,
  introduction = "这是一个示例统计分析报告。"
)
}

}
\seealso{
\code{\link[officer:read_docx]{officer::read_docx()}}, \code{\link[flextable:flextable]{flextable::flextable()}}
}
